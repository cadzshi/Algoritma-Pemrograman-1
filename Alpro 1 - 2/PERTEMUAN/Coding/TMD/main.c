#include "head.h"

int main ()
{
    scanf("%d", &jumlahk);
    for (int i = 0; i < jumlahk; i++)
    {
        scanf("%s", kalimat);    
    }

    scanf("%d", &jumlahtb);
    for (int i = 0; i < jumlahtb; i++)
    {
        scanf("%d", &transisibaris);
    }

    scanf("%d", &jumlahtp);
    for (int i = 0; i < jumlahtb; i++)
    {
        scanf("%d", &transisipesan);
    }

    scanf("%d", &tebal);

    
    return 0;
}