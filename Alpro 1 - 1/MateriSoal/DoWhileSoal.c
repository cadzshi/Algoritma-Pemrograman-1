#include <stdio.h>
#include <string.h>

/*//menghitung jumlah huruf konsonan
int main()
{
    char str[100];
    scanf("%s", &str);
    int i, konsonan = 0;
    for(i = 0;i < strlen(str);i++)
    {
        if(str[i] != 'a' && str[i] != 'i' && str[i] != 'u' && str[i] != 'e' && str[i] != 'o')
        {
            konsonan++;
        }
    }
    printf("%d\n", konsonan);
    return 0;
}
*/
